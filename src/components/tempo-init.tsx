"use client";

// This is a replacement for the missing tempo-init component
// It provides a minimal implementation to avoid build errors

import React from "react";

export function TempoInit() {
  // In a production environment, this would initialize the Tempo error tracking
  // For now, we're just providing a non-functional component to fix build errors
  
  return null; // Renders nothing to the DOM
}
